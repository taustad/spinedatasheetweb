import React, { FC } from "react"
import { Typography } from "@equinor/eds-core-react"

const UserManuals: FC = () => (
    <div>
        <Typography variant="h4">UserManuals</Typography>
    </div>
)

export default UserManuals